class Solution {
public:
    int countSeniors(vector<string>& details) {
        int ans=0;
        for(int i=0;i<details.size();i++)
        {       string z=details[i];
                char x=(z[11]);
                char y=(z[12]);
                int a=x-'0';
                int b=y-'0';
                int age=(10*a)+b;
                cout<<age<<endl;
                if(age>60)
                {
                    ans++;
                }
        }
        return ans;
    }
};
